name: Publish
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: VirtusLab/scala-cli-setup@v1.12.3
      - name: Run tests
        run: scala-cli --power test .
      - name: Publish
        run: |
          scala-cli --power publish . \
            --user "env:SONATYPE_USERNAME" \
            --password "env:SONATYPE_PASSWORD" \
            --gpg-key ${{ secrets.PUBLISH_GPG_KEY }} --gpg-option --pinentry-mode --gpg-option loopback
